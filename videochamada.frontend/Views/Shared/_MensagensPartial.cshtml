@using VideoChatApp.FrontEnd.Controllers
@{ var mensagem = TempData[GenericController.KeyMensagemAlerta] as string; }
@if (!string.IsNullOrEmpty(mensagem)) {
<div class="alert alert-danger">@mensagem</div>
}